pipeline {
    agent any
    triggers {
        pollSCM('* * * * *')
    }

    stages {
        stage('Test') {
            steps {
                script {
                    echo 'Checking Pre-requisites'
                    sh '''
                    python3 --version
                    newman --version
                    robot TestingFW.robot
                    '''
                }
            }
        }
    }
}
